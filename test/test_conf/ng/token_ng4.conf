events {
    events {}
}

http {
    server {
      listen 80; {
      server_name example.com www.example.com;

      root /var/www/html;
    }

    location /some-directory/ {
      autoindex on;; { aaa }
    }
}