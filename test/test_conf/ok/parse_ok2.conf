http {
    # CGI server
    server {
        listen          4242;
        listen          8484 default_server;
        server_name     cgi_server;
        root            www;

        location ^~ /cgi-bin/ {
           # CGIの設定
        }
    } # server

} # http
