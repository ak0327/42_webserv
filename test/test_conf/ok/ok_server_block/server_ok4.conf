http {
    server { server_name a; }
    server { server_name b; }
    server { server_name c; }
    server { server_name d; }
    server { server_name e; }
    server { server_name f; }
    server { server_name g; }
    server { server_name h; }
    server { server_name i; }
    server { server_name j; }
    server { server_name k; }
    server { server_name l; }
}
